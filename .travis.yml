sudo: required

language: generic

services: 
  - docker

before_install:
  - echo "start Creating an iamge with dockerfile"
  - docker build -t 1dilumn0/docker-react -f dockerfile.dev

script:
  - docker run -e CI=true 1dilumn0/docker-react npm run test -- -coverage

after_success:
  - echo "Test Success"